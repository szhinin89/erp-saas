# Features — ERP SaaS

> Estado de delivery por módulo: [`STATUS.md`](STATUS.md) · Arquitectura y rutas API: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)

## Módulos producto (scope: empresa — `company_id`)

| Dominio | Rutas / API | Notas |
|---------|-------------|-------|
| Auth & acceso | `/login`, `/api/auth/*`, `/api/me` | JWT + refresh rotation, RBAC |
| Master data — Business Partners (Customer/Supplier) | `/api/master/business-partners*` | BP V2, roles Customer/Supplier — FROZEN (ver `STATUS.md`) |
| Catálogo / Ítems | `/api/v1/catalog/*`, `/api/v1/items/*` | 14 entidades, 56 endpoints, CRUD completo, variantes, atributos, SRI lookups. Empaques con presentación base obligatoria para inventariables, validación de factor, bloqueo de empaques usados y advertencias visuales sin inferencia automática. FROZEN v2.0 (rediseño flujo de creación, 2026-07-02) + auditoría por fases 1-9 (2026-07-02) |
| Motor de Pricing v2 | — (consumido internamente vía `IPricingResolver`) | `Item.BaseSalePrice` SSOT + `PricingRule` (reemplaza `ItemPrice`) + `PriceList` con regla general opcional. CLOSED (ADR-021, 2026-07-05) — ver `STATUS.md` |
| Tipos de Ítem | `/api/v1/item-types`, `/inventory/item-types` | Catálogo tenant-editable (reemplaza enum fijo); `items.item_type_id` FK por Guid; sin flags de comportamiento. FROZEN |
| Inventario | `/api/inventory/*` (categorías, líneas, tipos, marcas, tarifas) | Company isolation FROZEN |
| Compras | `Modules/Purchases` | Compra XML con checklist previo a confirmación, bloqueo fail-closed de líneas inventariables sin presentación, asociación proveedor+código+presentación y alerta de costo base extremo contra último/promedio. |
| Ventas | `/api/v1/sales/*`, `/sales` | SalesInvoice + Detail; Draft→Authorize→Cancel; snapshot fiscal IVA/ICE; DocumentSequence SRI; facturación electrónica. FROZEN |
| Devolución de Ventas (SalesReturn) + Nota de Crédito SRI | `/api/v1/sales/returns*`, `/sales/returns` | Devolución parcial/total sobre factura autorizada; Draft→Update→Cancel→Authorize; control de remanente bajo concurrencia (advisory lock); reversión de inventario; reembolso explícito Efectivo/Crédito CxC/mixto; asiento contable automático (ADR-026); Entity Audit; Nota de Crédito electrónica SRI V1.1.0 + RIDE (ADR-031). **P0-01 — COMPLETED/CLOSED (2026-07-31)**, ver `STATUS.md` |
| Configuración / SRI | `/api/settings/*`, catálogos SRI | Sucursales, geografía, parámetros |
| Sucursales | `/api/v1/settings/branches`, `/settings/branches`, `/settings/branches/:id` | CRUD + soft-disable; organizativas, no fiscales. FROZEN |
| Establecimientos SRI | `/api/v1/settings/establishments`, `/settings/establishments` | Código fiscal SRI (001-999) único por empresa; BranchId opcional; disable bloqueado si tiene PEs activos. FROZEN |
| Puntos de Emisión | `/api/v1/settings/emission-points`, `/settings/emission-points` | Código 001-999 único por Establecimiento; DocumentSequence automático. FROZEN |
| Acceso / Seguridad | `/api/admin/iam`, `/api/security`, `/api/admin/activity`, `/api/v1/admin/access/sessions*`, `/admin/access/sessions`, `/api/v1/admin/iam/company-users/{id}/preferences`, `/api/v1/admin/iam/memberships`, `/api/v1/admin/iam/memberships/revoke`, `/api/v1/admin/iam/memberships/{id}/branches`, `/admin/users` | Perfiles, permisos, auditoría; UserSession (contexto operativo empresa/sucursal/terminal) — ver `docs/IDENTITY.md#usersession-contexto-operativo-del-usuario`; CompanyUserPreferences (sucursal por defecto + modo de login por usuario, UI en `/admin/security`) — ver `docs/IDENTITY.md#companyuserpreferences-preferencias-operativas-de-login`; **`/admin/users` — administración completa de `CompanyUserMembership`** (alta/edición de rol y perfil, sucursales autorizadas, preferencias de login, revocación/reactivación; `UsersPage`, Fase I-C, ver `STATUS.md`); **Security Hardening Fase S1** (ver `STATUS.md`) — `POST /api/v1/auth/register` y `POST /api/v1/auth/password-reset` eliminados (registro anónimo cross-tenant y reset de password sin verificación); alta de usuarios exclusivamente vía `/api/v1/setup/admin` (first-run) + administración de memberships; reset de password exclusivamente vía `forgot-password`/`reset-password` (token por email); endpoints admin de memberships/branches/preferences ahora exigen `IRequiresCompanyContext` (mismo patrón ya usado por alta/revocación de membership) |
| Contabilidad (Accounting Core — fundamentos) | `/api/v1/accounting/accounts*`, `/api/v1/accounting/accounting-periods*`, `/api/v1/accounting/posting-rules*` | ADR-026. CRUD completo de Plan de Cuentas, Períodos Contables y Reglas de Contabilización (crear, listar, consultar por Id/código, editar Name/ParentAccountId/AllowsPosting, activar/desactivar, cerrar/bloquear período según corresponda) |
| Contabilidad (visibilidad de asientos — ACCOUNTING-LEDGER-VISIBILITY-01) | `GET /api/v1/accounting/journal-entries`, `GET /api/v1/accounting/journal-entries/{id}`, `GET /api/v1/accounting/journal-entries/by-source/{sourceModule}/{sourceDocumentId}` | Solo lectura: listado paginado con filtros (Status/rango de fecha/SourceModule), detalle con líneas (AccountCode/AccountName resueltos contra el Plan de Cuentas) y bandera `IsBalanced`, búsqueda por documento de origen. Sin Create/Update/Delete — el Posting Engine (§8) sigue siendo la única vía de escritura. Frontend: `/accounting/journal-entries` (listado) y `/accounting/journal-entries/:id` (detalle), `frontend/src/modules/accounting/`. Brecha conocida ya resuelta por ACCOUNTING-SOURCE-TRACEABILITY-04 (ver fila siguiente) |
| Contabilidad (Plan de Cuentas — ACCOUNTING-CHART-OF-ACCOUNTS-02) | `GET /api/v1/accounting/accounts`, `GET /api/v1/accounting/accounts/{id}`, `GET /api/v1/accounting/accounts/by-code/{code}`, `POST /api/v1/accounting/accounts`, `PATCH /api/v1/accounting/accounts/{id}`, `PATCH /api/v1/accounting/accounts/{id}/enable`, `PATCH /api/v1/accounting/accounts/{id}/disable` | `UpdateAccountCommand` reemplaza al antiguo `RenameAccountCommand` (sin consumidores): edita Name/ParentAccountId/AllowsPosting — Code/AccountType/Nature quedan inmutables tras crear (clasificación universal, no editable post-facto). Validación de padre agregada en Application (diferida por diseño en `Account.cs`, ver `<remarks>`): existencia + misma Company en Create, más detección de ciclos y auto-referencia en Update. `AccountDto` ahora incluye `ParentAccountCode`/`ParentAccountName`/`Level` (calculado, 0 = raíz). `DisableAccountCommand` bloquea (Conflict) si la cuenta está referenciada por una `PostingRule` activa (DebitAccountId/CreditAccountId/Lines) — no bloquea por asientos ya contabilizados (historial). Frontend: `/accounting/chart-of-accounts` (`ChartOfAccountsPage.tsx`) — tabla con indentación por nivel, filtros tipo/estado/búsqueda, alta/edición/activar-desactivar |
| Contabilidad (auditoría de reglas de contabilización — ACCOUNTING-POSTING-RULES-AUDIT-03) | Sin endpoints nuevos — hardening de `POST/PATCH /api/v1/accounting/posting-rules*` y del Posting Engine interno | Cierra la brecha reportada en ACCOUNTING-CHART-OF-ACCOUNTS-02: **`PostingAccountGuard`** (nuevo, `ERP.Application/Modules/Accounting/Posting`) corre en el pipeline justo antes de `JournalFactory` y exige que cada cuenta de `PostingRule.Lines` exista, sea de la misma Company/Tenant, esté activa y tenga `AllowsPosting=true` — fail-closed, sin asiento parcial (código `POSTING_ACCOUNT_INVALID`, log estructurado con SourceModule/SourceEventType/SourceEventId/PostingRuleId). `CreatePostingRuleHandler`/`UpdatePostingRuleHandler` aplican la misma validación al guardar (falla rápido en vez de esperar al primer hecho real) y `CreatePostingRuleCommand` ahora exige `Lines.Count >= 2` ("líneas efectivas") — los campos legacy `DebitAccountId`/`CreditAccountId` se siguen aceptando y validando por compatibilidad, pero `JournalFactory` nunca los lee (documentado en el nuevo XML doc de `CreatePostingRuleCommand`). No se tocó JournalEntry ya posteado, el Posting Engine de Sales/Purchases, ni el plan de cuentas seed. Brecha documentada, no resuelta: ningún guard estructural impide que un Account con hijos tenga `AllowsPosting=true` (no existe esa regla de dominio hoy; no se inventó una migración destructiva sin confirmación) |
| Contabilidad (trazabilidad de origen — ACCOUNTING-SOURCE-TRACEABILITY-04) | Sin endpoints nuevos — enriquece `JournalEntryListItemDto`/`JournalEntryDetailDto` de los endpoints ya existentes de `journal-entries*` | Origen documental humano resuelto en tiempo de lectura (nunca persistido en `JournalEntry`, nunca toca asientos históricos): `SourceDocumentType`/`SourceDocumentNumber`/`SourceDocumentDate`/`SourcePartyName`/`SourceStatus`/`SourceRoute`, todos nulos si no se puede resolver. **`IJournalEntrySourceResolver`** (composite, Strategy) despacha por `SourceModule` hacia `SalesJournalSourceResolver`/`PurchaseJournalSourceResolver` (`ERP.Application/Modules/Accounting/Queries`), registrados en DI como `IJournalEntrySourceModuleResolver` — agregar un módulo nuevo es una línea de registro, sin tocar el composite. Cada resolver de módulo inyecta directamente `ISalesInvoiceRepository`/`IPurchaseInvoiceRepository` (mismo patrón ya usado por Finance en `ApplySupplierCreditUseCases`) vía el nuevo método batch `GetJournalSourceSummariesByIdsAsync` en cada repo — sin exponer la entidad de dominio, solo tuplas. Solo resuelve FactType `InvoiceIssued` (Sales) e `InvoiceReceived` (Purchases) — `SalesReturn`/`PurchaseReturn`/`SupplierCredit*` quedan sin resolver deliberadamente (brecha, extensión aditiva futura); Finance (`CollectionApplied`/`SupplierPaymentApplied`) se agregó en ACCOUNTING-CASH-POSTING-06 (ver fila siguiente). Frontend: listado agrega columnas Tipo/Número documento, Tercero, Fecha documento (fallback a SourceModule/SourceEventId técnico si no resuelto); detalle agrega bloque "Documento origen" con link de navegación real (`/sales?invoiceId=`/`/purchases?invoiceId=`, mismo patrón ya usado por "Ver documento origen" desde Kardex) o aviso "Origen documental no disponible" |
| Contabilidad (reversos — ACCOUNTING-REVERSALS-05) | Sin endpoints nuevos — `ReverseJournalEntryCommand` (ya existía, Fase 5.4) sigue sin HTTP endpoint, deliberado: solo se dispara desde el nuevo translator, nunca manualmente desde la UI (sin caso de uso/permiso definido para reverso manual) | Auditoría confirmó que `ReverseJournalEntryCommand`/`JournalEntry.Reverse()` (Fase 5.4) no tenían ningún consumidor real — solo tests. **`PurchaseInvoiceCancelledPostingTranslator`** (nuevo) es el primer flujo real: al anular una `PurchaseInvoice` ya contabilizada (`PurchaseInvoiceCancelledEvent`, que ya existía pero solo tenía un handler de auditoría), localiza el `JournalEntry` original vía `GetBySourceAsync("Purchases","InvoiceReceived",invoiceId)` y lo reversa (nunca crea un asiento compensatorio nuevo — a diferencia de SalesReturn/PurchaseReturn, que sí publican un PostingFact independiente porque una devolución es un documento distinto). Log-and-continue: un reverso fallido nunca revierte la anulación de la compra. **`SalesInvoice.Cancel()` no levanta ningún domain event — brecha documentada, no inventada** (la venta anulada hoy no tiene ningún efecto contable). Purchase Credit Notes tampoco tienen translator — mismo criterio, brecha reportada. `JournalEntryDetailDto` ahora incluye `OriginalJournalEntryNumber`/`Date` y `ReverseJournalEntryNumber`/`Date` (resueltos en `GetJournalEntryByIdHandler`, 0-2 lookups extra); si el propio asiento no resuelve origen documental (un reverso siempre lleva `SourceModule="Accounting"`/`SourceEventType="Reversal"`, ver `JournalEntry.Reverse()`), hereda el origen del asiento original. Frontend: `JournalEntryDetailPage` agrega card "Reverso contable" (badge "Es reverso de otro asiento" / "Tiene un asiento reverso", número+fecha del asiento vinculado, botón "Ver asiento relacionado") — sin botón de reverso manual |
| Contabilidad (cobros/pagos/caja — ACCOUNTING-CASH-POSTING-06) | Sin endpoints nuevos | Auditoría: `CollectionAppliedPostingTranslator`/`SupplierPaymentAppliedPostingTranslator` (Finance/`CollectionApplied`+`SupplierPaymentApplied`) **ya existían y ya postean correctamente** (Fase 5.6.1) — GrandTotal transporta el monto, un único `PostingRule` fijo por `(Company, SourceModule, FactType)` resuelve las cuentas (idempotencia/`PostingAccountGuard`/`PostingPeriodGuard` ya aplican genéricamente vía el pipeline, sin código nuevo). Brecha estructural confirmada, no resuelta: ni `PostingFact` ni `CollectionAppliedEvent`/`SupplierPaymentAppliedEvent` cargan `PaymentMethodId`/`FinancialDestinationId` — "Debe: Caja/Banco según método de pago" no es representable hoy, todo cobro/pago enruta a una única cuenta fija sin importar el método (`Payment.PaymentMethodId` existe en el aggregate pero se descarta antes de levantar el evento). El módulo **Caja (CashSession/CashRegister/CashMovement) no levanta ningún domain event** — apertura/cierre/movimientos manuales no tienen ningún efecto contable hoy; no se inventó un evento nuevo (fuera de alcance sin confirmación). Nuevo: **`FinanceJournalSourceResolver`** (`ERP.Application/Modules/Accounting/Queries`) resuelve origen humano para `CollectionApplied`/`SupplierPaymentApplied` vía el nuevo método batch `IPaymentRepository.GetJournalSourceSummariesByIdsAsync` (Company+Tenant scoped, a diferencia de Sales/Purchases que solo scopean por Tenant — señal `IJournalEntrySourceResolver`/`IJournalEntrySourceModuleResolver` ahora reciben `companyId` explícito) + `IBusinessPartnerRepository.GetNamesByIdsAsync` para el nombre de cliente/proveedor (BP V2, mismo repo para ambos roles); `Payment` no tiene numeración documental propia (no es comprobante SRI) — solo resuelve si `Payment.Reference` fue capturado, nunca inventa un número; sin `SourceRoute` (no existe hoy una vista de detalle de Payment con deep link seguro). Sin cambios de frontend — la card "Documento origen" ya existente en `JournalEntryDetailPage` renderiza "Cobro de cliente"/"Pago a proveedor" automáticamente en cuanto el backend resuelve el origen |
| Contabilidad (costo de ventas/inventario — ACCOUNTING-INVENTORY-COGS-07) | Sin endpoints nuevos | Auditoría: Inventory (`StockMovement`/`CurrentStock`) **ya calcula costo promedio móvil real** (Kardex, `RunningAverageCost`/`RunningStockValue`) pero no levanta ningún domain event — cero acoplamiento a Accounting, tal como exige el proyecto. Fix mínimo en `StockRepository.CreateAndTrackMovementAsync` (Infrastructure, no Domain): las salidas (venta/ajuste) resolvían su costo desde el promedio corrido pero nunca lo guardaban en `StockMovement.UnitCost`/`TotalCost` (quedaban `null`) — ahora sí, sin cambiar ningún otro comportamiento (las entradas con costo explícito, p. ej. compras, no cambian). Nuevo: **`SalesInvoiceCogsPostingTranslator`** — segundo `INotificationHandler` del ya existente `SalesInvoiceAuthorizedEvent` (mismo patrón que `PurchaseInvoiceAuditHandler`+`PurchaseInvoiceConfirmedPostingTranslator`, sin inventar ningún evento) — resuelve el costo consultando `IStockRepository.GetMovementsByDocumentAsync(invoiceId,"SalesInvoice")` y suma `TotalCost` (Kardex, nunca `GrandTotal`/precio de venta); si es cero (venta solo de servicios) no publica nada. FactType `Sales/CostOfGoodsSold`, monto en `PostingAmountKind.HistoricalCost` (reutilizado, mismo kind que Purchase Returns — sin enum nuevo). Nuevo: **`SalesReturnCogsReversalPostingTranslator`** — segundo handler de `SalesReturnAuthorizedEvent`, FactType `Sales/CostOfGoodsSoldReversed`, Debe Inventario/Haber Costo (dirección fijada por la `PostingRule` del admin, no por código) — nunca usa `JournalEntry.Reverse()` (una devolución es un documento nuevo, mismo criterio que SalesReturn de ingresos, no una anulación). `SalesJournalSourceResolver` extendido para resolver también `CostOfGoodsSold` (mismo SourceEventId que `InvoiceIssued` — misma factura). Compras: confirmado sin doble contabilización — `Purchases/InvoiceReceived` nunca setea `HistoricalCostTotal`, combinación `(SourceModule,FactType)` totalmente distinta de `Sales/CostOfGoodsSold`, cero solapamiento posible. Brecha documentada, no resuelta: Purchase Credit Notes/`CostOfGoodsSoldReversed` (SourceEventId=SalesReturn.Id) siguen sin resolver origen humano (mismo criterio ya documentado para "SalesReturn" desde ACCOUNTING-SOURCE-TRACEABILITY-04) |
| Contabilidad (notas de crédito de compra — ACCOUNTING-CREDIT-NOTES-POSTING-08) | Sin endpoints nuevos | Cierra la brecha reportada en ACCOUNTING-CASH-POSTING-06: `PurchaseCreditNoteAuthorizedEvent`/`PurchaseCreditNoteCancelledEvent` (FLOW-READY-02C §4.3) existían desde antes con doc comment explícito de "punto de extensión inerte... una fase futura debe darle efecto contable explícitamente" — este ticket es esa decisión, sin modificar ninguno de los dos eventos (solo se actualizó su comentario XML para reflejar que ya no están inertes). Solo cubre `PurchaseCreditNoteApplicationType.Discount` (el único tipo que `PurchaseCreditNote.Authorize()` permite) — el tipo `Return` sigue siendo responsabilidad exclusiva de `PurchaseReturnAuthorizedPostingTranslator` (ya existente), sin duplicación posible. Nuevo: **`PurchaseCreditNoteAuthorizedPostingTranslator`**, FactType `Purchases/PurchaseCreditNoteAuthorized` — Debe CxP (`AppliedToPayableAmount`)/Haber cuenta base (`Subtotal`)+IVA crédito tributario (`VatAmount`), montos ya resueltos por la entidad (`RecalculateTotals()`), nunca recalculados. Nuevo: **`PurchaseCreditNoteCancelledPostingTranslator`** — mismo criterio que `PurchaseInvoiceCancelledPostingTranslator` (ACCOUNTING-REVERSALS-05): reversa el asiento original vía `ReverseJournalEntryCommand` si `AppliedToPayableAmount` no es null (la NC llegó a autorizarse); si es null (cancelada desde Draft) no hay nada que reversar. `PurchaseJournalSourceResolver` extendido (mismo SourceModule="Purchases" que "InvoiceReceived", nueva rama para "PurchaseCreditNoteAuthorized"/"PurchaseCreditNoteCancelled") vía el nuevo método batch `IPurchaseCreditNoteRepository.GetJournalSourceSummariesByIdsAsync` + `IBusinessPartnerRepository.GetNamesByIdsAsync`; ruta real `/purchases/credit-notes/{id}` (route ya existente). Brecha de `IceAmount` cerrada en ACCOUNTING-PURCHASE-CREDIT-NOTE-ICE-08B (ver fila siguiente). Brecha aún abierta: SalesReturn/PurchaseReturn siguen sin resolver origen humano (ambos serían "si aplica" per ticket — se evaluó y se decidió no implementar en esta fase: requieren un repositorio de lookup nuevo cada uno, sin método batch existente, alcance dejado para una fase futura explícita) |
| Contabilidad (ICE en notas de crédito de compra — ACCOUNTING-PURCHASE-CREDIT-NOTE-ICE-08B) | Sin endpoints nuevos | Cierra la brecha reportada en ACCOUNTING-CREDIT-NOTES-POSTING-08: `PurchaseCreditNote.IceAmount` ya existía (calculado por `RecalculateTotals()` desde `TaxSummaries`, congelado al autorizar) pero nunca se propagaba a `PurchaseCreditNoteAuthorizedEvent`, así que `PurchaseCreditNoteAuthorizedPostingTranslator` siempre fijaba `TotalIce=0m` sin importar el ICE real del documento. Fix: `IceAmount` agregado al FINAL de la lista posicional del constructor del evento (aditivo puro, con default `0m` — el único call site, `PurchaseCreditNote.Authorize()`, ya lo pasa explícitamente; ningún test/serialización existente se rompió). El translator ahora usa `e.IceAmount` (nunca recalcula) para `PostingFact.TotalIce`. **Sin cuenta ICE hardcodeada**: qué cuenta recibe el crédito ICE es 100% configuración de `PostingRule` (`PostingAmountKind.TaxIce`, ya existente, sin enum nuevo) — si la NC no tiene ICE (`IceAmount=0`), la línea ICE de la regla (si el admin la configuró) se omite automáticamente vía `JournalFactory` (nunca contabiliza monto cero) — la MISMA `PostingRule` sirve para NC con y sin ICE, sin lógica condicional en el translator. `SalesReturn`/`PurchaseReturn` no se tocaron (fuera de alcance, restricción explícita del ticket) |
| Contabilidad (diagnóstico: base local sin datos contables — ACCOUNTING-DATA-SEED-AND-SMOKE-10G) | Sin cambios de código — diagnóstico puro | Confirmado con evidencia SQL directa (`dberpsaas`, puerto 5435): `accounts=0`, `accounting_periods=0`, `posting_rules=0`, `journal_entries=0` en las 3 companies de la base local, mientras que operación real SÍ existe (`sales_invoices=70` con 45 Authorized, `purchase_invoices=3` Confirmed, `stock_movements=64`). `purchase_invoice_audit` tiene 3 filas `action='Confirmed'` — prueba directa de que `PurchaseInvoiceConfirmedEvent` sí se disparó las 3 veces (mismo evento que consume `PurchaseInvoiceConfirmedPostingTranslator`). Causa raíz: sin ninguna `Account`/`PostingRule` en toda la base, `PostingRuleResolver` nunca encuentra regla para ningún `(Company, SourceModule, FactType)` — la posting falla y el translator hace *log-and-continue* exactamente como está diseñado (nunca revierte la venta/compra). No es bug de UI/filtros/tenant — es la ausencia total de configuración contable inicial (Caso A del ticket). Se descartó `company.is_accounting_req=false` como causa (es un atributo fiscal SRI para XML/RIDE, sin relación con el Posting Engine). Empresa con actividad real en ambos flujos: **ZH TECH** (`d81f5a1c-be13-4308-97b9-0a8f19d35d65`). Siguiente ticket: `ACCOUNTING-INITIAL-CHART-SEED-11` (ver fila siguiente) |
| Contabilidad (seed inicial de Plan de Cuentas y Períodos — ACCOUNTING-INITIAL-CHART-SEED-11) | Sin endpoints — solo seeding backend | Cierra Caso A de 10G. **Patrón elegido**: se reutilizó el mecanismo `ICompanyBootstrapStep` ya establecido (mismo patrón que `CajaBootstrapStep`/`InventoryBootstrapStep`/etc., "Tipo A" — infraestructura genérica, editable después, nunca datos de negocio inventados) — nuevo **`AccountingBootstrapStep`** (`CompanyBootstrapStepOrder.Accounting=47`) crea, por Company, 13 cuentas mínimas (Activo×5/Pasivo×3/Patrimonio×2/Ingresos×1/Costos×1/Gastos×1, `AccountType`/`Nature` correctos por sección, todas `IsActive=true`/`AllowsPosting=true`, sin jerarquía padre/resumen) + 1 `AccountingPeriod` anual abierto (`PeriodNumber=1`, año calendario actual, `01-01`..`12-31`) — idempotente por `Code` y por `FiscalYear`, registrado en DI junto a los demás steps (aplica automáticamente a toda Company nueva desde ahora). Como `ICompanyBootstrapStep` solo corre al crear una Company, **no** alcanzaba a las 3 companies ya existentes en la base local (ZH TECH incluida) — se agregó **`AccountingChartBackfillService`** (nuevo, dev-only vía `!IsProduction()`, sin flag adicional por ser puramente aditivo/idempotente, mismo criterio de gating que `E2ESeedService`), invocado desde `Program.cs` en cada arranque: busca companies activas sin ninguna cuenta y les aplica el mismo `AccountingBootstrapStep` (reutilizado, no duplicado) — genérico por Company, nunca hardcodeado a un CompanyId. **Verificado en vivo**: se detuvo/reconstruyó/reinició la API local dos veces — primera corrida sembró 13 cuentas + 1 período para las 3 companies (incluida ZH TECH); segunda corrida confirmó cero duplicados (mismo conteo exacto). Sin `PostingRules` todavía (fuera de alcance explícito, ver `ACCOUNTING-POSTING-RULES-SEED-11B`) ni asientos retroactivos. `.gitignore`-adjacent: se agregaron los 2 archivos nuevos al `ignoreQueryFiltersAllowlist` de `architecture-rules.json` (mismo `.IgnoreQueryFilters()` ya permitido para todos los demás bootstrap steps, necesario porque corren fuera de un HttpContext con tenant ambiente) |
| Contabilidad (auditoría DS completa del módulo — ACCOUNTING-DS-FULL-AUDIT-10F) | Sin cambios de backend — 100% frontend | Extiende el fix de 10E (solo Reportes) a las 4 pantallas restantes del módulo. **`/accounting` (AccountingHubPage.tsx)**: auditada, sin hallazgos — ya usa clases propias reales (`accounting-hub.css`, prefijo `acc-`) y componentes DS correctos. **`/accounting/journal-entries`**: `pg-flex pg-gap-8` (×2, ghost class ya identificada en 10E) → `zh-form-actions-row`; faltaba `import ".../items-catalog.css"` (`.prd-sku` del código de documento origen truncado no tenía estilo). **`/accounting/chart-of-accounts`**: mismo fix de `pg-flex pg-gap-8` (×2) → `zh-form-actions-row`; el resto de la pantalla (formularios Crear/Editar con `ZHGrid`+`ZHField`) ya cumplía DS, sin otros hallazgos. **`/accounting/journal-entries/:id`** (el hallazgo más grande): toda la pantalla dependía de `.sr-general-grid`/`.sr-general-grid__label`/`.sr-general-grid__value` — **clases sin ninguna definición en los 4 stylesheets globales del proyecto** (mismo patrón de clase fantasma que `pg-flex`, pero afectando el 100% del layout de esta pantalla: 4 bloques de datos completos sin estilo real) — reemplazadas por `ZHInfoRow`+`ZHFieldLabel`+`ZHDataValue` (trío real del DS para pares label/valor, mismo patrón ya probado en `StockTransferPage.tsx`); faltaba también el import de `items-catalog.css` (`.prd-sku`/`.prd-empty-row`); montos de la tabla de líneas migrados de `formatMoney()` a `ZHMoneyValue` por consistencia con el resto del módulo. La tabla de líneas se mantiene `<table>` nativa (clases reales `table table--compact table--neutral` de `zh-ui.css`) porque necesita `<tfoot>` de totales — `ZHDataTable` no expone ese slot, no es un raw-HTML evitable. **Confirmado sin cambios de lógica**: ningún endpoint, cálculo, query ni regla contable tocada — solo JSX/className/imports de CSS en 4 archivos. QA responsive (1440/1024/768): `.zh-filterbar`/`.zh-grid`/`.card-header` ya traen `flex-wrap`/breakpoints reales de `zh-ui.css`, sin CSS local nuevo que pudiera romperlos |
| Contabilidad (fix visual DS de Reportes — ACCOUNTING-REPORTS-DS-QA-FIX-10E) | Sin cambios de backend — 100% frontend | **Dos problemas visuales reales encontrados** (no solo percepción): (1) `AccountingReportsPage.tsx` nunca importaba `styles/shared/items-catalog.css` (donde viven `.prd-tabs`/`.prd-tab-btn`, las clases que `ZHTabBar` usa) — a diferencia de `ChartOfAccountsPage.tsx`/`BranchDetailPage.tsx`, que sí lo hacen; por code-splitting de rutas, si `/accounting/reports` era la primera pantalla del módulo visitada, sus tabs cargaban sin ningún estilo (root cause real de "tabs con apariencia HTML nativa"). (2) Los 6 archivos usaban `pg-flex`/`pg-gap-8`/`pg-gap-16`/`pg-pad-8` para casi todo layout de filtros/botones/totales — **ninguna de esas 4 clases está definida en NINGÚN stylesheet del proyecto** (verificado por grep exhaustivo en `design-tokens.css`/`legacy-pages.css`/`page-template.css`/`zh-ui.css`); son clases fantasma heredadas de tickets anteriores (mismo patrón repetido, sin efecto, en otros 15+ archivos del ERP — fuera de alcance de este ticket, queda como hallazgo para una futura limpieza). **Fix**: filtros migrados de `ZHCard actions` (slot de 2-3 campos, insuficiente para hasta 5 controles) a `ZHFilterBar` + `ZHField density="compact"` — mismo patrón ya probado en `ElectronicDocumentsFilters.tsx` (labels visibles, grid responsive real vía `.zh-filterbar__field` con `min-width`+`flex-wrap`); `pg-flex pg-gap-8` → `zh-form-actions-row` (real, definida); `zh-actions pg-gap-16 pg-pad-8` → `zh-actions` (ya trae gap+margin-top real); totales migrados de `formatMoney()` en `<strong>` a `ZHMoneyValue` (`emphasis="total"`/`"grand"` para utilidad bruta/neta y Total Activo/Pasivo+Patrimonio); secciones de Estado de Resultados/Balance General (Ingresos/Costos/Gastos, Activos/Pasivos/Patrimonio) pasan de `<strong>` plano a `ZHCard` anidado con `zh-section-title` (real, ya definida) + `zh-mb-16` para separación entre secciones/cuentas. Checkbox de Balance de Comprobación se mantiene HTML crudo (regla del proyecto: permitido fuera de `ZHToggle`) pero se asocia con `<label htmlFor>` y se integra al filterbar. Sin cambios de queries/cálculos/reglas contables/endpoints — verificado por diff: solo JSX/className, cero lógica de negocio tocada |
| Contabilidad (estructura de menú como módulo formal — ACCOUNTING-MODULE-MENU-STRUCTURE-FIX-10D) | Sin endpoints nuevos — `AccountingModule.cs` reestructurado (mismo archivo de ACCOUNTING-NAV-VISIBILITY-FIX-10C) | **Causa exacta**: 10C registró Contabilidad en `KernelRegistry`/menú real con UN solo `[NavItem]` ("Contabilidad" → `/accounting`, el hub de tarjetas de ACCOUNTING-MODULE-NAV-UX-10B) — técnicamente visible, pero en revisión manual se confirmó que esa UX (un solo ítem que obliga a pasar por un hub de tarjetas) no respeta el patrón real del resto del ERP, donde cada pantalla es un ítem de menú independiente (mismo criterio que `ProductsModule`: varios ítems planos bajo un módulo, sin container "Operación"/"Configuración" salvo que haga falta, a diferencia de `CajaModule`). **Fix**: el único `[NavItem]` se reemplaza por tres ítems planos — Asientos contables (`/accounting/journal-entries`), Plan de cuentas (`/accounting/chart-of-accounts`), Reportes (`/accounting/reports`) — cada uno apuntando directo a la pantalla real ya implementada, sin URLs nuevas. Los tres comparten `AccountingPermissions.View` deliberadamente: es el único permiso que hoy protege esos tres endpoints reales (`AccountingController`/`AccountingReportsController`) — no se inventaron permisos granulares (`accounting.journalEntries.view`/etc.) sin autorización de backend real detrás, para no crear un permiso hueco sin efecto de seguridad. Configuración contable/Períodos contables NO se agregan al menú (siguen sin CRUD real en frontend, restricción explícita del ticket). El hub `/accounting` (`AccountingHubPage.tsx`) NO se elimina — sigue existiendo como landing opcional alcanzable por URL directa o desde el botón "Contabilidad" de las 3 páginas — pero deja de ser la única navegación del módulo: el menú ahora entra directo a las 3 pantallas. Claves i18n renombradas (`app.nav.item.accounting.hub` → `journalEntries`/`chartOfAccounts`/`reports`) en `es.json`/`en.json`/`qu.json`. Test de `KernelRegistryTests.cs` reemplazado por `Navigation_contains_accounting_journal_entries_chart_of_accounts_and_reports_as_independent_items` — verifica las 3 rutas+permiso y que `/accounting` YA NO aparece como ítem de menú (solo como página alcanzable por URL). Sin tocar Posting Engine, reportes, plan de cuentas, asientos, ni endpoints — 100% estructura de menú |
| Contabilidad (fix de visibilidad en el menú principal — ACCOUNTING-NAV-VISIBILITY-FIX-10C) | Sin endpoints nuevos — un `[Module]`/`[NavItem]` nuevo en `ERP.Domain.Kernel.Modules` | **Causa raíz exacta**: el ERP tiene DOS mecanismos de navegación independientes — (1) `AppFeatureDiscoveryService` sincroniza `app_features` desde `[AppFeature]` de los controllers, pero eso solo alimenta el catálogo de permisos (pantalla de administración de roles), y encima requiere el flag `Development:SyncFuncionalidadesOnStartup=true` (default `false`) para correr; (2) el menú lateral REAL (`GET /api/v1/me/menu` → `NavigationBuilder` → tablas `ui_nav_groups`/`ui_nav_items`) se arma exclusivamente desde `KernelRegistry.Navigation`, derivado por reflexión de clases `[Module]`/`[NavItem]` en `ERP.Domain.Kernel.Modules` — sincronizado a esas tablas por `NavigationSyncService` en CADA arranque (paso obligatorio de bootstrap, sin flag). `AccountingController` siempre tuvo `[AppFeature]` (por eso el catálogo de permisos SÍ mostraba "Contabilidad" y `accounting.view` funcionaba correctamente en los endpoints) pero NUNCA tuvo un `[Module]`/`[NavItem]` — así que `KernelRegistry.Navigation` jamás lo incluyó y el menú lateral real nunca pudo mostrarlo, sin importar que la ruta React `/accounting` (ACCOUNTING-MODULE-NAV-UX-10B) y el permiso `accounting.view` (preexistente, ya usado por todos los endpoints del módulo desde ACCOUNTING-LEDGER-VISIBILITY-01) estuvieran correctos. **Fix**: `AccountingModule.cs` nuevo (`ERP.Domain/Kernel/Modules/`) — un único `[NavItem]` plano ("Contabilidad", `Permission = AccountingPermissions.View`, ruta `/accounting`), mismo patrón que `ProductsModule` (varios ítems planos sin contenedor "Operación"/"Configuración" cuando no hace falta, a diferencia de `CajaModule`); Id `ac000000-…` (prefijo nuevo, sin colisión con los ya usados `a1/b1/b2/c1/d1/e2/e3/e4/f5/f6/f7`, verificado por grep antes de fijarlo). Sin permiso nuevo (`accounting.view` ya existía y ya protegía todos los endpoints — la falla era 100% de registro de navegación, no de permisos, confirmado por `NavigationBuilder.cs`: rol Admin ya bypassea todo permiso, y para roles no-Admin la resolución de permisos efectivos es la misma vía ya usada por el resto del ERP, sin necesidad de tocarla). Claves i18n nuevas (`app.nav.group.accounting`/`app.nav.item.accounting.hub`) agregadas en `es.json`/`en.json`/`qu.json` (las 3, exigido por el check `i18n-keys`). Tests nuevos en `KernelRegistryTests.cs`: `Navigation_contains_accounting_hub_entry` (ruta+grupo+permiso correctos) y `Modules_contains_accounting_module` — ambos ejercitan el MISMO código (`KernelRegistry`) que consume `/api/v1/me/menu` en producción, dando garantía equivalente a un QA manual de login sin depender de una sesión de navegador interactiva (no disponible en este entorno de ejecución — se deja explícito, no se fabricó un resultado de click-through). Sin tocar lógica contable, reportes, asientos, plan de cuentas, reglas de contabilización ni Posting Engine — 100% registro de navegación |
| Contabilidad (hub de navegación del módulo — ACCOUNTING-MODULE-NAV-UX-10B) | Sin cambios de backend (ticket 100% frontend) | `/accounting` deja de redirigir directo a Asientos y ahora muestra un hub (`AccountingHubPage.tsx`, mismo patrón grid-de-tarjetas ya establecido por `InitialLoadHubPage.tsx`) con una tarjeta por sección: Asientos contables, Plan de cuentas, Reportes (las 3 ya operativas, con descripción breve de qué hace cada una) y Configuración contable/Períodos contables como "Próximamente" — sus endpoints (`posting-rules*`/`accounting-periods*`) ya existen en el backend pero no tienen pantalla propia todavía (brecha reportada, no un olvido; no se creó ninguna pantalla vacía grande para ellas, solo la tarjeta deshabilitada). Las 4 páginas del módulo (`JournalEntriesPage`/`ChartOfAccountsPage`/`AccountingReportsPage`/`JournalEntryDetailPage`) ganan `kicker="Contabilidad"` (prop ya existente de `PageShell`) y un botón "Contabilidad" que vuelve al hub, dando sensación de módulo formal sin tocar el backend (`[AppFeature]` de `AccountingController` sigue siendo una única entrada de nav — no soporta submenús; el hub ES la estructura de navegación del módulo dentro de esa restricción). Nuevo `accounting-hub.css` con prefijo `acc-` (reservado para el módulo en `css-prefixes.json` desde antes, sin CSS previo en el módulo). Sin cambios de lógica contable, sin nuevos reportes, sin cierre contable — 100% navegación/UX. Brecha de navegación documentada: Configuración contable y Períodos contables seguirán como "Próximamente" hasta que exista un ticket dedicado a construir esas pantallas (Posting Rules CRUD / Accounting Periods CRUD) |
| Contabilidad (estados financieros básicos — ACCOUNTING-FINANCIAL-STATEMENTS-10) | `GET /api/v1/accounting/reports/income-statement`, `GET /api/v1/accounting/reports/balance-sheet` (nuevas), más `general-journal`/`general-ledger`/`trial-balance` migradas sin cambio de ruta | Primera versión de Estado de Resultados y Balance General, ambos 100% solo lectura sobre `JournalEntry`/`JournalEntryLine` `Posted` — reutilizan `IJournalEntryRepository.GetAccountLineTotalsAsync` (ya existente desde ACCOUNTING-REPORTS-09, sin cambios) para Σ Debit/Σ Credit por cuenta, cero SQL nuevo. **`AccountType` gana un valor nuevo, `Cost`** (agregado al FINAL del enum — nunca insertado entre valores existentes, porque `AccountConfiguration` lo mapea a `int` plano en BD sin CHECK constraint; insertarlo antes de `Expense` habría corrido su valor persistido y corrompido cuentas ya guardadas), necesario para separar Costo de Ventas de Gastos operativos en el Estado de Resultados (clasificación estándar del Plan de Cuentas ecuatoriano, grupo 5 vs grupo 6) — extensión aditiva pura, sin lógica condicional nueva; `ChartOfAccountsPage`/`accountSchema.ts` (frontend) y `.IsInEnum()` (backend) la soportan automáticamente. Estado de Resultados: solo período (`FromDate`..`ToDate`, sin saldo inicial — las cuentas de resultados no arrastran saldo entre rangos en este sistema), agrupa Ingresos/Costos/Gastos, `UtilidadBruta = TotalIncome − TotalCost`, `UtilidadNeta = UtilidadBruta − TotalExpense`. Balance General: saldo acumulado (`fromDate=null` hasta `AsOfDate` inclusive) de Activo/Pasivo/Patrimonio, `Difference = TotalAssets − (TotalLiabilities + TotalEquity)`, `IsBalanced = Difference == 0`. **Reversos**: mismo criterio ya probado en ACCOUNTING-REPORTS-09 — un asiento reversado queda `Reversed` (excluido), su reverso `Posted` cuenta como movimiento normal; probado end-to-end con Postgres real para ambos reportes nuevos. **Brecha documentada, no resuelta (deliberada)**: sin cierre contable, Balance General puede legítimamente mostrar `IsBalanced=false` mientras exista actividad de Ingresos/Costos/Gastos sin cerrar a Patrimonio — el frontend muestra un `ZHPageNotice` explicando esto, nunca oculta la diferencia; NO se implementó un traslado sintético de utilidad a Patrimonio (habría sido inventar una regla contable nueva, restricción explícita del ticket). **Split de controller**: `AccountingReportsController` nuevo (`Controllers/Accounting/`, mismo prefijo `api/v1/accounting/reports`, sin `[AppFeature]` propio) — las 3 rutas de reportes de ACCOUNTING-REPORTS-09 se movieron ahí sin cambiar su URL ni su lógica (split mecánico, mismo patrón ya usado en Purchases), y las 2 nuevas se agregaron junto a ellas; `AccountingController.cs` bajó de 281 a 214 líneas (sigue en warning, ya no crece más por reportes). Frontend: 2 tabs nuevos en `/accounting/reports` (`IncomeStatementReportTab.tsx`/`BalanceSheetReportTab.tsx`), mismo patrón ZH ya establecido. Brechas explícitas fuera de alcance (ninguna implementada, tal como pide el ticket): Flujo de Efectivo, cierre contable, exportación Excel/PDF, notas a estados financieros, comparativos por período |
| Contabilidad (fix DI de suites Testcontainers — ACCOUNTING-TESTCONTAINERS-DI-FIX-09C) | Sin endpoints — solo test setup | Corrige las 6 suites Testcontainers preexistentes identificadas al cierre de ACCOUNTING-REPORTS-09 (`SalesInvoiceAuthorizedPostingIntegrationTests`, `PurchaseInvoiceConfirmedPostingIntegrationTests`, `PurchaseReturnAuthorizedPostingIntegrationTests`, `CollectionPostingIntegrationTests`, `SupplierPaymentPostingIntegrationTests`, `ApplySupplierCreditConcurrencyTests`) — confirmado que las 6 fallaban por `ServiceCollection` local incompleta, nunca por lógica funcional: `PostingEngine` requiere `IAccountRepository` desde ACCOUNTING-POSTING-RULES-AUDIT-03 (`PostingAccountGuard`), pero ninguna de las 6 lo registraba, y `SalesInvoiceAuthorizedPostingIntegrationTests` además nunca registró `IStockRepository`/`IDatabaseExceptionTranslator` que exige el segundo handler de `SalesInvoiceAuthorizedEvent` (`SalesInvoiceCogsPostingTranslator`, ACCOUNTING-INVENTORY-COGS-07) que `AddMediatR` descubre por escaneo de ensamblado. Fix: una línea `services.AddScoped<IAccountRepository, AccountRepository>();` en las 6 (mismo patrón ya usado para las demás dependencias del bloque), más `IStockRepository`/`IDatabaseExceptionTranslator` (implementación real `PostgresDatabaseExceptionTranslator`, sin estado) solo en la suite de Sales. Sin tocar ningún handler productivo, entidad de dominio, evento ni regla de contabilización — 100% test setup. Resultado: 39/39 en las 6 suites (antes 25 fallos), 37/37 en toda la categoría Accounting/PostgreSql (antes 25/37), `ERP.Application.Tests` sin cambios (1157/1157), `dotnet build`/`architecture:check` sin nuevas violaciones (mismo baseline de 25) |
| Contabilidad (reportes de auditoría — ACCOUNTING-REPORTS-09) | `GET /api/v1/accounting/reports/general-journal`, `GET /api/v1/accounting/reports/general-ledger`, `GET /api/v1/accounting/reports/trial-balance` | Primera superficie de auditoría contable: Libro Diario, Libro Mayor y Balance de Comprobación, los tres 100% solo lectura sobre `JournalEntry`/`JournalEntryLine` ya `Posted` — cero recálculo desde Ventas/Compras/Inventario/Finanzas. Reglas de inclusión: `Status == Posted` explícito en las tres queries — Draft excluido siempre; un asiento `Reversed` (el original) queda excluido automáticamente porque `JournalEntry.Reverse()` ya cambia su propio `Status` a `Reversed` al crear el reverso — su reverso (que SÍ es `Posted`) cuenta como un asiento normal más, sin lógica especial (probado en `AccountingReportsRepositoryTests` con Testcontainers real). Nuevo en `IJournalEntryRepository`: `GetPostedEntriesPageAsync` (Libro Diario, paginado, filtro opcional `SourceModule`/`search` por N° asiento o descripción — sin buscar por documento origen todavía, brecha documentada), `GetAccountLineTotalsAsync` (Σ Debit/Σ Credit por cuenta agregado en SQL vía GroupBy, usado dos veces por reporte: saldo inicial con rango abierto hacia atrás + movimiento del período), `GetPostedLinesByAccountAsync` (detalle Kardex de una cuenta, proyecta directo a `JournalEntryLineReportRow` sin cargar el `JournalEntry` completo — evita N+1 y sobre-fetch). Libro Mayor calcula saldo inicial/saldo acumulado según `Account.Nature` (Debit: Σ Debit − Σ Credit; Credit: al revés); Balance de Comprobación usa la convención contable neutral (neto positivo → columna deudora, negativo → columna acreedora, independiente de `Nature`) con bandera `IsBalanced` si el total Debe del período ≠ total Haber. `IJournalEntrySourceResolver` reutilizado sin cambios (ya batchea por `SourceModule`, sin necesidad de extenderlo) para "documento origen" en Libro Diario y Libro Mayor. Frontend: nueva sección **Contabilidad → Reportes** (`/accounting/reports`, `AccountingReportsPage.tsx` + 3 tabs vía `ZHTabBar`, sin componentes nuevos del Design System) con acceso desde el botón "Reportes" ya existente en `JournalEntriesPage.tsx`. Tests nuevos: 5 en `ERP.Application.Tests` (agregación mockeada: sumas Libro Diario, saldo inicial/final Libro Mayor, balance Balance de Comprobación, inclusión/exclusión de cuentas sin movimiento) + 3 de integración PostgreSQL real en `ERP.Infrastructure.Tests` (exclusión de Draft, reverso Posted vs original Reversed, aislamiento multi-company). Brechas explícitas fuera de alcance (ninguna implementada, tal como pide el ticket): Balance General, Estado de Resultados, Flujo de Efectivo, cierre contable, exportación Excel/PDF. Hallazgo no relacionado, pre-existente, detectado al cierre de este ticket: 6 suites de integración PostgreSQL de tickets anteriores fallaban con `Unable to resolve service for type IAccountRepository/IStockRepository` en su `ServiceCollection` local — no corregido en este ticket por estar fuera de su alcance; resuelto en el ticket siguiente, ACCOUNTING-TESTCONTAINERS-DI-FIX-09C (ver fila siguiente) |
| Contabilidad (Posting Engine — Fase 3.1) | `IPostingEngine` (`ERP.Application/Modules/Accounting/Posting`) | ADR-026 §8. Motor de contabilización interno (sin endpoints todavía): `PostingFact` → resolución de `PostingRule`/`AccountingPeriod` → `JournalFactory` construye `JournalEntry` con líneas reales (`JournalEntryLine`, Fase 3.5.5) → `JournalValidator` verifica balance → persistencia, idempotente por `(CompanyId, SourceModule, SourceEventId, FactType)` vía advisory lock (Fase 3.3.5). Sin `Post()`/`Reverse()`, sin numeración `JournalEntrySequence` |
| Contabilidad (primer consumidor real — Fase 3.3) | `SalesInvoiceAuthorizedPostingTranslator` (`ERP.Application/Modules/Accounting/Posting/Translators`) | Ventas ya contabiliza automáticamente al autorizar una factura: `SalesInvoice.Authorize()` → `SalesInvoiceAuthorizedEvent` → Translator → `IPostingEngine` → `JournalEntry` Draft. Fallo de Posting nunca revierte la venta (log-and-continue). Motor integrado con idempotencia concurrente real (advisory lock PostgreSQL, Fase 3.3.5) — probado con Caja y Accounting reaccionando simultáneamente al mismo evento sin conflicto |
| Contabilidad (segundo consumidor real — Fase 3.4) | `PurchaseInvoiceConfirmedPostingTranslator` (`ERP.Application/Modules/Accounting/Posting/Translators`) | Compras ya contabiliza automáticamente al confirmar una factura: `PurchaseInvoice.Confirm()` → `PurchaseInvoiceConfirmedEvent` → Translator → `IPostingEngine` → `JournalEntry` Draft. Mismo patrón exacto que Ventas (Fase 3.3): fallo de Posting nunca revierte la confirmación de la compra (log-and-continue), sin partida doble ni montos/impuestos en el `PostingFact` todavía. Retenciones (`IssuedWithholding`) fuera de alcance — hecho contable distinto |
| Contabilidad (PostingFact Enrichment — Fase 3.5.2) | `PostingFact` (`ERP.Application/Modules/Accounting/Posting`) | ADR-026 §4 completado: `SalesInvoiceAuthorizedEvent`/`PurchaseInvoiceConfirmedEvent` y `PostingFact` ahora llevan `Subtotal`/`TotalVat`/`TotalIce`/`TotalDiscount`/`GrandTotal` — montos ya resueltos por Sales/Purchases, nunca recalculados por Accounting. Prerrequisito para el motor de partida doble (`JournalEntryLine`, diseño en Fase 3.5.1) — `JournalFactory` todavía no consume estos montos, sigue generando solo el encabezado del asiento |
| Contabilidad (modelo de dominio de partida doble — Fase 3.5.3) | `JournalEntryLine`, `PostingRuleLine`, `PostingAmountKind` (`ERP.Domain/Modules/Accounting/Entities`, `Enums`) | Modelo de dominio, con persistencia (Fase 3.5.4) y ahora consumido por el Posting Engine (Fase 3.5.5) |
| Contabilidad (persistencia de partida doble — Fase 3.5.4) | `journal_entry_lines`, `posting_rule_lines` (migración `20260725165737_AddJournalEntryLineAndPostingRuleLine`) | `JournalEntryLine.AccountId` con FK real a `accounts` (Restrict); `PostingRuleLine.AccountId` sin FK (configuración, igual criterio que `PostingRule.DebitAccountId`/`CreditAccountId`). Cascade delete de líneas al eliminar su encabezado. `DebitAccountId`/`CreditAccountId` de `PostingRule` intactos — coexistencia deliberada durante la transición |
| Contabilidad (motor de partida doble real — Fase 3.5.5) | `JournalFactory`, `JournalValidator` (`ERP.Application/Modules/Accounting/Posting`) | Sales y Purchases ya generan asientos con líneas reales de Débito/Crédito, no solo el encabezado: `JournalFactory` construye una `JournalEntryLine` por cada `PostingRuleLine` de la regla configurada, resolviendo el monto exclusivamente por `PostingAmountKind` (Subtotal/TaxVat/TaxIce/Discount/GrandTotal — Retention aún no disponible en `PostingFact`). `JournalValidator` rechaza (código `VALIDATION_FAILED`) asientos con menos de 2 líneas, cuenta duplicada en Débito y Crédito, montos inválidos o desbalanceados. Requiere `PostingRule` con al menos 2 `PostingRuleLine` configuradas por `(Company, SourceModule, FactType)` — sin ellas, fail-closed |

## Plataforma SaaS (scope: tenant — `tenant_id`)

| Feature | Ruta | Notas |
|---------|------|-------|
| Empresas | `/companies`, `/api/companies` | Sin UUID en URL (sessionStorage `erp.saas.*`) |
| Tenant / onboarding | `Modules/Tenants`, `/api/setup` | Contrato SaaS — ver [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md#jerarquía-multiempresa) |

> La capa de planes/billing/entitlements descrita en versiones anteriores de este documento fue **eliminada en FASE 1 — ERP Kernel Cleanup** (2026-06-05). No existen rutas `/superadmin/*` ni `/api/saas/*` activas — ver [`STATUS.md`](STATUS.md) y [`docs/archive/SAAS-COMMERCIAL.md`](docs/archive/SAAS-COMMERCIAL.md) (histórico).

## i18n

Español, English, **Kichwa de Cañar (`qu`)**.
