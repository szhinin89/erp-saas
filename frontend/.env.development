# Vacío = en dev las peticiones van a este mismo host (Vite) y vite.config.ts
# reenvía /api/* a la API en localhost:5003 → sin problemas de CORS (localhost vs 127.0.0.1, otro puerto, etc.).
VITE_API_URL=
