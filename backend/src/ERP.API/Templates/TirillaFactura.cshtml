@model ERP.Application.Sales.Models.ReceiptModel
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>Tirilla de Factura</title>
    <style>
        /* Tirilla tÃ©rmica: ancho segÃºn ConfiguracionFacturacion.ReceiptWidth (58 o 80 mm) */
        body {
            width: @(Model.Configuration.ReceiptWidth + 8)mm;
            max-width: @(Model.Configuration.ReceiptWidth + 8)mm;
            margin: 0;
            padding: 8px 4px;
            font-family: "Segoe UI", Arial, sans-serif;
            font-size: 12px;
            color: #000;
            box-sizing: border-box;
        }

        .ticket {
            width: 100%;
            max-width: @(Model.Configuration.ReceiptWidth)mm;
        }

        .center {
            text-align: center;
        }

        .bold { font-weight: 700; }
        .small { font-size: 11px; }
        .line { border-top: 1px dashed #000; margin: 8px 0; }
        .spacer { margin-top: 12px; }

        .header img {
            max-width: 100%;
            height: auto;
            margin-bottom: 8px;
        }

        .invoice-meta,
        .customer-meta,
        .totals {
            width: 100%;
        }

        .invoice-meta td,
        .customer-meta td,
        .totals td {
            vertical-align: top;
            padding: 2px 0;
        }

        .items {
            width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
        }

        .items th,
        .items td {
            padding: 2px 0;
        }

        .items th {
            border-bottom: 1px solid #000;
        }

        .items td.description {
            text-align: left;
        }

        .items td.quantity,
        .items td.price,
        .items td.total {
            text-align: right;
            white-space: nowrap;
        }

        .barcode {
            margin-top: 12px;
            text-align: center;
            font-size: 11px;
            letter-spacing: 1px;
        }

        @@media print {
            body { margin: 0; padding: 0; }
            .ticket { page-break-inside: avoid; }
        }
    </style>
</head>
<body>
    <section class="ticket">
        <div class="header center">
            @if (!string.IsNullOrWhiteSpace(Model.Configuration.LogoBase64))
            {
                <img src="data:image/png;base64,@Model.Configuration.LogoBase64" alt="Logo" />
            }
            <div class="bold">@Model.Configuration.TradeName</div>
            <div class="small">@Model.Configuration.LegalName</div>
            <div class="small">RUC: @Model.Configuration.IdentificationNumber</div>
            <div class="small">@Model.Configuration.Address</div>
            <div class="small">Tel: @Model.Configuration.Phone</div>
            @if (!string.IsNullOrWhiteSpace(Model.Configuration.Email))
            {
                <div class="small">@Model.Configuration.Email</div>
            }
        </div>

        <div class="line"></div>

        <table class="invoice-meta small">
            <tr>
                <td>Factura:</td>
                <td class="bold">@Model.Invoice.DocType-@Model.Invoice.EstabCode-@Model.Invoice.EmPointCode-@Model.Invoice.Sequential</td>
            </tr>
            <tr>
                <td>Fecha:</td>
                <td>@Model.Invoice.IssueDate.ToString("dd/MM/yyyy HH:mm")</td>
            </tr>
            <tr>
                <td>Estado:</td>
                <td>@Model.Invoice.Status</td>
            </tr>
            @if (!string.IsNullOrWhiteSpace(Model.Invoice.AuthNumber))
            {
                <tr>
                    <td>AutorizaciÃ³n:</td>
                    <td>@Model.Invoice.AuthNumber</td>
                </tr>
            }
        </table>

        <div class="line"></div>

        <table class="customer-meta small">
            <tr>
                <td>Cliente:</td>
                <td class="bold">@(Model.Buyer?.Name.TradeName ?? Model.Buyer?.Name.LegalName ?? "CONSUMIDOR FINAL")</td>
            </tr>
            <tr>
                <td>Identificación:</td>
                <td>@(Model.Buyer?.Identification.Type ?? "") @(Model.Buyer?.Identification.Number ?? "9999999999")</td>
            </tr>
        </table>

        <div class="line"></div>

        <table class="items small">
            <thead>
                <tr>
                    <th class="description">DescripciÃ³n</th>
                    <th class="quantity">Qty</th>
                    <th class="price">P.U.</th>
                    <th class="total">Total</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var line in Model.Invoice.Lines)
                {
                    <tr>
                        <td class="description">@line.Description</td>
                        <td class="quantity">@line.Quantity</td>
                        <td class="price">@line.UnitPrice.ToString("N2")</td>
                        <td class="total">@line.Total.ToString("N2")</td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="line"></div>

        <table class="totals small">
            <tr>
                <td>Subtotal</td>
                <td class="total">@Model.Invoice.Subtotal.ToString("N2")</td>
            </tr>
            <tr>
                <td>Impuesto</td>
                <td class="total">@Model.Invoice.TaxTotal.ToString("N2")</td>
            </tr>
            <tr class="bold">
                <td>Total</td>
                <td class="total">@Model.Invoice.Total.ToString("N2")</td>
            </tr>
        </table>

        <div class="line"></div>

        @if (!string.IsNullOrWhiteSpace(Model.Configuration.FooterText))
        {
            <div class="small center">@Model.Configuration.FooterText</div>
        }

        @if (Model.IsTestMode)
        {
            <div class="small center bold spacer">FACTURA DE PRUEBA</div>
        }

        @if (!string.IsNullOrWhiteSpace(Model.Invoice.AccessKey))
        {
            <div class="barcode">@Model.Invoice.AccessKey</div>
        }
    </section>
</body>
</html>


