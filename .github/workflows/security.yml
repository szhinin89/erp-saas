name: Security checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  identity:
    name: Identity guardrails
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Identity guardrails (no legacy users auth)
        shell: pwsh
        run: ./tools/architecture/check-identity-guardrails.ps1
