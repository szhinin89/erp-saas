# Infrastructure — monitoring (placeholder)

Configuración ops de observabilidad (enlace con [`monitoring/`](../../monitoring/) en la raíz).

| Carpeta | Uso |
|---------|-----|
| `grafana/` | Dashboards exportados (futuro) |
| `prometheus/` | Scrape config (futuro) |

Stack actual del producto: Serilog + health checks API — ver [`monitoring/README.md`](../../monitoring/README.md).
